import CloudinaryUpload from "@/components/CloudinaryUpload";

/** Routed at `/uploadimage` — full-page Image Vault with header chrome. */
export default function CloudinaryuploadPage() {
  return <CloudinaryUpload showHeader />;
}
